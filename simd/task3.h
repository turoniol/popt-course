#pragma once

/**
 * Perform vector addition and compute the dot product of two vectors using SIMD
 * intrinsics. Specifically, write functions that add two large arrays of
 * floating-point numbers using SSE/AVX intrinsics and optimize a dot product
 * calculation routine using SIMD instructions. Implement the task in both C++
 * and Assembly language.
 */
void run_task3();